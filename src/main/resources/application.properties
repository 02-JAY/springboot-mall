spring.datasource.url=jdbc:mysql://localhost:3306/mall?serverTimezone=Asia/Taipei&characterEncoding=utf-8
spring.datasource.username=${DB_USERNAME:}
spring.datasource.password=${DB_PASSWORD:}

jwt.secret=${JWT_SECRET:}

line.auth.client-id=${LINE_CLIENT_ID:}
line.auth.client-secret=${LINE_CLIENT_SECRET:}
line.auth.redirect-uri=${LINE_REDIRECT_URI:}